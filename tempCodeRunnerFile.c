#include <stdio.h>

int main() {
    long a = 1000000;       // Equivalent to long int
    signed char u = 'a';
    long int b = 2000000;   // Full form

    printf("a = %ld\n", a);
    printf("b = %ld\n", b);
    printf("%c",u);
    return 0;
}
